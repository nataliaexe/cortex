# Portfolio Frontend

Static portfolio built with HTML, CSS and Three.js.

## Run locally

Open [index.html](index.html) directly in a browser, or use a simple static server such as:

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000.
