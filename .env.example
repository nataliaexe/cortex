# Do not commit .env. Override non-secret local settings here when needed.
CORTEX_CONFIG=config.yaml
# Example for a future PostgreSQL deployment; SQLite is the current default.
# CORTEX_DATABASE_URL=postgresql://user:password@localhost/cortex
